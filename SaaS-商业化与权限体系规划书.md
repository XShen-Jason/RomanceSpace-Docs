# 💼 RomanceSpace SaaS 商业化与权限体系规划书

这份文档是我们从“工具网站”走向“正式 SaaS 收费平台”的核心蓝图。请根据你的实际成本承受能力，在这里调整各项参数。

## 一、 关于登录认证成本的硬性核算

针对你提出“微信扫码”和“手机号验证码登录”的需求，这里是现实世界中的成本考量：

### 1. 🟢 [最推荐方案 - 零成本起步] 邮箱注册 / 魔法链接登录
*   **成本**：全免费（Supabase 每月提供 5 万个活跃用户的免费额度）。
*   **适用人群**：海内外通杀。特别是对需要长久保存自己爱情网页的用户，他们更愿意绑定长期邮箱。
*   **优势**：无需“企业营业执照”认证，个人开发者立刻就能开通上线。也可以附带 Google、GitHub 一键授权（同样无审核门槛）。

### 2. 🟡 [手机号短信验证码登录]
*   **成本**：**按条收费**（阿里云/腾讯云大约 0.035~0.045 元/条短信）。
*   **门槛问题**：国内短信平台现在要求极其严格的**实名认证与企业资质**才能申请“短信签名”。个人开发者极大可能申请不到，或者只能申请个人测试签名（限制极其严苛）。
*   **黑客风险**：如果没有挂强大的图形验证码（如 Cloudflare Turnstile 防护），会有被“短信轰炸机”盗刷几十万短信费用的可怕风险（俗称刷量）。

### 3. 🔴 [微信扫码登录 (网页端)]
*   **成本**：微信开放平台“网站应用”需要交纳 **300元/年** 的主体认证费。
*   **门槛问题**：必须拥有**企业营业执照**！个人无法申请官方的“网页微信扫码登录”。个人目前只能搞微信小程序，而我们的平台是网页。

**🚀 初始阶段的战略建议**：
初期严禁使用短信或微信（这会死磕资质和大量现金成本）。建议采用 **邮箱账号密码** 或 **Supabase 魔法邮箱链接**（点一下邮件直接登入），不仅高大上，而且完全免费零门槛，防止被恶意盗刷费用。

---

## 二、 核心转化漏斗：匿名体验首位 (Anonymous Builder)

针对工具类 SaaS，如果在首页强制弹窗要求“注册/登录”，至少会有 40% 的流量流失。
**改良互动流程**：
1. **完全开放的首页与画廊**：用户直接能点进 Builder，挑选他想要的模板。
2. **无需登录的实时预览**：用户随意填词、放照片（BSR 架构完全在此刻是浏览器本地运算，0 服务器成本）。
3. **收网时刻（发布绑定）**：他把情话写到最感动的时候，点击“生成我的专属网页”。此时系统弹出登录/注册框，告知：“登录以永久保存并领取专属短域名”。
*   **技术红利**：得益于我们的 React BSR 架构，前 2 个步骤我们的服务器不产生一丝费用，甚至后端都不知道他在体验，却能大幅度拉升最终的用户转化率。

---

## 三、 账户权限与邀请裂变防作弊体系 (Fraud Prevention)

仅仅有邀请码是不够的，羊毛党用脚本刷账号会让我们损失珍贵的极品短域名。

### 1. 邀请码强绑定机制
*   **锁定机制**：当 B 通过 A 的链接 `.../register?ref=CODE_A` 访问时，注册框强制填入 A 的邀请码且不可修改，确保地推人员的绝对利益。

### 2. 反制脚本刷量 (Anti-Cheating)
*   **IP 维度的硬限流**：注册接口 `POST /api/auth/register` 同一 IP 每 24 小时最多只允许注册 3 个账号。
*   **奖励延迟发放机制（极其重要）**：B 注册成功时，A **不获取** 任何奖励（如免费修改次数或高级额度）。只有当 B 被强制引导进入 Builder 页面并**“首次成功发布了一个非空白模板的网站”** 时，系统才会在数据库触发 Webhook，给 A 确切下发分佣奖励。

### 3. 管理员专属黑屋 (Admin Upload Portal)
*   通过给 Supabase `profiles` 表挂隐藏标记（`role: 'admin'`），在 `https://www.885201314.xyz/admin/upload` 取决重重防御。

---

## 四、 阶梯会员制、资源额度与域名回收 (Quota & Garbage Collection)

我们是一个生成静态页面的平台。**服务器不值钱，域名才值钱**。我们的收费逻辑应当围绕“名额”与“修改特权”展开。你可以参考下面这套设计好的梯队：

### 🌟 免费用户（流量贡献层）
通过裂变注册进来的普通人。这层人要帮我们冲热度，顺便帮我们给 Pro 会员铺路。
*   **获得专属域名数量**：`1 个`
*   **发布条件**：必须进入 Builder 页面完成填表并成功发布后才激活该域名。
*   **修改限额 (Strict Quota)**：
    *   **更新网页内容 (改字/换图)**：每天仅限 `3 次`。
    *   **更换模板款式 (换皮肤)**：每周仅限 `1 次`。
*   **品牌展示（推广机制）**：模板底部强制显示一行精美的“制作同款浪漫网页”跳转链接（非强制加粗水印）。
*   **回收规则**：若连续 `180 天` 零访问，域名将被系统自动回收。

### 💎 Pro 会员 (商业盈利层)
针对有更高定制需求的用户。
*   **获得专属域名数量**：`3 - 5 个`。
*   **修改限额**：内容修改每天 `20 次`，模板更换每天 `5 次`。
*   **品牌控制**：可自主选择是否隐藏底部的“制作同款”跳转链接，实现完全私有化。
*   **模板限制**：解锁所有高级 3D / 特殊视差模板。

### 👑 终身合伙人 / 一次性买断层 (高溢价, 例如 199元)
主打不用每个月操心续费的土豪。
*   **获得专属域名数量**：永久 `10 个` 域名名额。
*   **修改次数**：每天无限修改额度（触发后台队列限速防刷即可）。
*   **终极特权 (自定义独立域名)**：利用 Cloudflare for SaaS 功能，允许该等级用户将他们自己在阿里云买的域名（如 `love-lily.com`）直接 CNAME 指向我们的 Worker 节点。

### 🗑️ 域名占用风控与自动化回收 (Recycle System)
*   **防恶意占位**：“域名输入框”进行 0.5s 实时探活防重复后，如果在 10 分钟内心跳未检测到用户点击“正式发布”并入库，该短域名锁强制释放，还给市场。
*   **长期休眠回收清理**：每月跑一次 Cron 脚本清理僵尸。如果某个免费用户的生成网页在过去 180 天内其数据访问统计为 0（彻底被遗忘的“死网站”），系统给原主发一封警告邮件，3天后自动解绑该超短域名，清空 R2 静态页面，释放公用资源池。

---

## 五、 为什么我们【坚决不限制访问流量】？（架构哲学冲突点）

有外部声音建议：“免费用户的页面，每月被访问 2000 次后就跳出弹窗要求他们充值，否则限流”。
**这个逻辑在普通云服务器上是对的，但在我们的 CQRS Edge 架构上是灾难性的错误。**

1.  **极高昂的计费成本**：我们的前端网页一旦生成，就被永远地甩在 Cloudflare 全球 CDN 网络中了。如果要去“限制访问次数”，意味着每次访客打开网页，你的 Worker 都需要向 Cloudflare KV（甚至是远端数据库）发出 1 次**“写入操作”**来记录加法。
2.  **KV Write 的昂贵**：CF 的读取每天免费十万次，但写入极少！一个爆红的分手贴导致 100 万人围观，如果你去做计流器限制他，你一天产生的 API 写入费将瞬间破产。
3.  **我们的哲学**：“读取 (View)”的成本近乎等于零。我们应该鼓励无尽地分享这千万级免费流量（带着我们平台底部的引流水印）。我们唯一卡死、限制的护城河必须是 **“写操作 (Render & Upload & Update)”**。流量免费吃，修改要花钱！

---

## 六、 核心工作流 (不写代码的交互流程图)

当你（管理员）在未来真正敲定产品后，一个典型用户的操作闭环是这样的：

1. **匿名入坑**：老赵没账号，来到首页，看中了情人节模板直接点进去放照片、写情话，预览效果惊叹。
2. **发布设限与透明告知**：老赵点“发布”，弹出框要求他注册。
    *   **温馨提示**：在注册页明确告知：“如果您连续 180 天没有任何访客查看此页面，为了节省公共域名资源，系统将自动回收该域名”。
3. **输入框实时输入防重名检测：** 老赵手输 `zhaoxiaomei` -> 系统 0.5s 后亮起绿灯。
4. **强行锁定**：老赵点击“确认发布” -> 域名正式写入名下。
5. **社交裂变**：前端跳出成功框：“您的专属纪念网页正式上线！底部带有您的专属推广链接”。如果是免费用户，该链接不可移除；如果是 Pro 且老赵非常有分享欲，他可以选择保留链接以赚取邀请奖励。

---

## 七、 技术解密：域名查重与模板切换的底层流转 (Technical Workflow)

为了确保系统的极致并发与不可篡改，我们需要理清“用户创建域名”和“二次修改模板”时的真实底层流转：

### 1. 用户的“第一次”：建站与域名争夺赛 (Domain Creation Payload)
当小白用户在平台首次填写完情话，点击“确认发布”并输入他想要的子域名（如 `love`）时，**绝大部分压力必须由 Supabase 和 VPS 记忆体硬扛**：
1. **[0延时] 内存黑名单校验**：VPS 收到请求首先查自己的内存 `__sys__blocklist`，看是不是保留字。是则直接拦截（不耗费任何请求钱）。
2. **[跨洋验证] Supabase 唯一性查库**：VPS 拿着特权 Key 连去 Supabase，执行 `SELECT user_id FROM projects WHERE subdomain = 'love'`。
   * **没查到**：好，这个域名没人占！顺便查查这名用户目前在库里有几个域名，没超额度就放行。进入生成环节。
   * **查到了**：如果查询出来的 `user_id` 不是该用户的 ID，直接报错 4010 弹回（“该前缀已被他人抢先使用”）。
3. **[写时双轨] 生成并入网**：
   * **CF 体系 (读的基石)**：将最终 HTML 覆盖写入 R2，并在 KV 路由表中记录 `love => { status: 1, template: type }`。
   * **Supabase 体系 (权限基石)**：最后，必须执行一次 `INSERT INTO projects`，把这笔产权永久落户在 Supabase 里。

### 2. 用户的“第N次”：日常换皮肤与改词儿 (Template Switching / Updating)
当用户觉得腻了，想把原本的 `love_letter` 模板**一键切换成高阶版**时，这属于**更新（Update）**操作。
1. **鉴权过检**：VPS 去查 Supabase 发现 `love` 域名确实是这个用户创建的（属于他的资产）。
2. **换皮重铸**：VPS 提取新选模板的骨架，把他的旧情话数据重新包裹渲染。把新生成的 HTML **直接强覆盖** 写入原先的 R2 路径下。
3. **KV 路由变更**：**重点就在这里！**由于模板换了，我们需要向 CF KV 下达**修改路由**的指令，覆盖原本的键值对：`love => { status: 1, template: '高阶版新模板' }`。这样 Worker 才知道他换了皮肤。
4. **Supabase 补录**：与此同时，必须执行一条 `UPDATE projects SET template_type='高阶版新模板'` 的 SQL 语句，通知业务数据库同步这一变更。
5. **CDN 清退**：调用 CF 全局 API，把这名用户原先的旧缓存彻底洗刷。

**核心哲学**：**Supabase 永远只做“安检员和账房先生”（判断归属，同步更新档案），它永远不经手最终重达几百 KB 的 HTML 文件；产生的大块头数据全部甩给 Cloudflare（KV/R2）。**

---

## 🚀 基础架构承载力分析报告 (2C2G VPS + CF Free)

基于我们目前的 **CQRS (读写分离)** 架构，对你目前的硬件配置进行的容量核算：

### 1. 核心瓶颈点：Cloudflare KV (Free 版)
*   **写操作限额**：1,000 次 / 天。
*   **这意味着**：你的全平台每天最多支撑 **1,000 次**【新页面创建】或【域名绑定变更】。
*   **读操作限额**：100,000 次 / 天。
*   **这意味着**：如果不经过 CDN 缓存，每天最多只能有 10 万人查看这些页面。但别担心，配合 Cloudflare 高级缓存，真实承载力会翻倍。

### 2. VPS (2C2G) 压力估算
*   **后端主要任务**：正则拼装、R2 上传。这是一个 **CPU 密集型** 任务。
*   **并发承载**：2 核心 CPU 可以同时处理大约 **20 - 50 个** 正在进行的渲染请求（Render）。
*   **用户量级**：由于用户不是每秒都在点“发布”，2C2G 支撑一个 **“日活 5,000 - 10,000”** 的平台游刃有余。
*   **内存消耗**：2G 内存对于 Node.js + Nginx 非常充裕，只要不处理巨大的 4K 图片上传就没问题。

### 3. R2 存储空间
*   **限额**：10 GB (Free)。
*   **单网页大小**：约 100 KB。
*   **存储上限**：约 **10 万个独立网页**。

### 📊 总结结论
**这套“穷人版”黄金组合，足以支撑你赚到第一个一万块。**
*   **日活上限**：约 5,000 ~ 10,000 人。
*   **新网页创建上限**：每天 1,000 个（受 KV 写入限制）。
*   **建议**：当你的日访客突破 5 万或者每天有超过 1,000 人创建页面时，只需要花费 5 美金/月 升级到 Cloudflare Workers Paid Plan 即可获得千万级读写，无需更换 VPS。
